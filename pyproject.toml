[project]
name = "turing-pi-cluster-ansible"
version = "0.1.0"
description = "Pinned Ansible controller environment for the DietPi Turing Pi cluster"
requires-python = ">=3.14,<3.15"
dependencies = [
  "ansible-core==2.21.3",
  "ansible-lint==26.8.0",
  "detect-secrets==1.5.0",
]

[tool.uv]
package = false
required-version = "==0.12.5"
